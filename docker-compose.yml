version: '3'
services:
  app:
    build:
      context: ./docker/php
      args:
        - TZ='Asia/Tokyo'
    volumes:
      - ./logs:/var/log/php
      - ./docker/php/php.ini:/usr/local/etc/php/php.ini